import statistics*

data=[]
n=input("Input the total number of items: ")
for inter in range(0,n):
    data.append=input("Input number : " str(inter))

meanx=statistics.mean(data)

print("The mean of the data is: "str(meanx))